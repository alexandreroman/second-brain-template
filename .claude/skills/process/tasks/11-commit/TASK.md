# 11. Commit Changes

- `git add notes/ obsidian/ backlog.txt`
- `git diff --cached --quiet -- notes/ obsidian/ backlog.txt` to check for staged changes.
- If changes exist: `git commit -m "Add content: <NOTE_TITLE>" -- notes/ obsidian/ backlog.txt`
