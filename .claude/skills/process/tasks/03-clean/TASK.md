# 3. Clean URL

- Run: `python <TASK_DIR>/clean_url.py "<URL>"`
- Variable: `SOURCE` = cleaned URL printed to stdout.

## Scripts

- `clean_url.py` — strips tracking parameters and normalizes the URL.
